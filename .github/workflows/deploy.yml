name: Deploy ComplianceOS to Alibaba Cloud ECS

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  IMAGE_NAME: ${{ secrets.ALIYUN_REGISTRY }}/${{ secrets.ALIYUN_REGISTRY_NAMESPACE }}/${{ secrets.ALIYUN_REGISTRY_REPO }}

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Log in to Aliyun Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ secrets.ALIYUN_REGISTRY }}
          username: ${{ secrets.ALIYUN_REGISTRY_USERNAME }}
          password: ${{ secrets.ALIYUN_REGISTRY_PASSWORD }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ env.IMAGE_NAME }}:latest

      - name: Deploy via Tailscale/ZeroTier alternative (webfactory/ssh-agent)
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.ALIYUN_ECS_SSH_KEY }}

      - name: Add remote server to known hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H ${{ secrets.ALIYUN_ECS_HOST }} >> ~/.ssh/known_hosts

      - name: Deploy natively via SSH (Minimal Connect)
        env:
          HOST: ${{ secrets.ALIYUN_ECS_HOST }}
          USER: ${{ secrets.ALIYUN_ECS_USER }}
          REGISTRY: ${{ secrets.ALIYUN_REGISTRY }}
          USERNAME: ${{ secrets.ALIYUN_REGISTRY_USERNAME }}
          PASSWORD: ${{ secrets.ALIYUN_REGISTRY_PASSWORD }}
          ZHIPU_API_KEY: ${{ secrets.ZHIPU_API_KEY }}
          IMAGE_NAME: ${{ env.IMAGE_NAME }}
        run: |
          # Use ssh with legacy ciphers to prevent kex_exchange_identification error on some Aliyun Ubuntu variants
          SSH_CMD="ssh -o KexAlgorithms=+diffie-hellman-group14-sha1,diffie-hellman-group-exchange-sha1,diffie-hellman-group1-sha1 -o HostKeyAlgorithms=+ssh-rsa,ssh-dss -o PubkeyAcceptedKeyTypes=+ssh-rsa $USER@$HOST"
          
          # 1. SSH in and create directories
          echo "=== 1. Creating directories ==="
          $SSH_CMD "mkdir -p /opt/compliance-os-v2/{data,config,logs}"
          
          # 2. SSH in, generate docker-compose.yml
          echo "=== 2. Generating docker-compose.yml ==="
          $SSH_CMD "cat > /opt/compliance-os-v2/docker-compose.yml" << EOF
          services:
            web:
              image: $IMAGE_NAME:latest
              container_name: compliance-os-v2-web
              restart: always
              ports:
                - "3001:3000"
              environment:
                - NODE_ENV=production
                - DATABASE_URL=file:/app/data/prod.db
                - ZHIPU_API_KEY=$ZHIPU_API_KEY
              volumes:
                - /opt/compliance-os-v2/data:/app/data
          EOF
          
          # 3. Final SSH to execute docker commands
          echo "=== 3. Pulling and deploying containers ==="
          $SSH_CMD "
            set -e
            cd /opt/compliance-os-v2
            chmod 777 data
            
            # Login and deploy
            echo '$PASSWORD' | docker login $REGISTRY -u '$USERNAME' --password-stdin
            
            if docker compose version >/dev/null 2>&1; then
              docker compose pull && docker compose up -d
            else
              docker-compose pull && docker-compose up -d
            fi
            
            docker image prune -f
          "
